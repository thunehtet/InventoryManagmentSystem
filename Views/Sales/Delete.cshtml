@using ClothInventoryApp.Dto.Sale
@model ViewSaleDto

@{
    ViewData["Title"] = "Delete Sale";
}

<div class="create-product-shell">
    <div class="create-main-card">
        <div class="section-header">
            <div>
                <h3>Delete Sale</h3>
            </div>
        </div>

        <div class="form-grid-modern">
            <div class="form-group-modern">
                <label class="form-label-modern">Sale Date</label>
                <input value="@Model.SaleDate.ToString("yyyy-MM-dd")" class="input-modern" readonly />
            </div>

            <div class="form-group-modern">
                <label class="form-label-modern">Total Amount</label>
                <input value="@Model.TotalAmount" class="input-modern" readonly />
            </div>
        </div>

        <hr />

        <table class="table-modern">
            <thead>
                <tr>
                    <th>Variant</th>
                    <th>Quantity</th>
                    <th>Unit Price</th>
                    <th>Cost Price</th>
                    <th>Line Total</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Items)
                {
                    <tr>
                        <td>@item.ProductVariantName</td>
                        <td>@item.Quantity</td>
                        <td>@item.UnitPrice</td>
                        <td>@item.CostPrice</td>
                        <td>@item.LineTotal</td>
                    </tr>
                }
            </tbody>
        </table>

        <form asp-action="Delete" method="post" class="mt-4">
            <input type="hidden" asp-for="Id" />
            <div class="form-footer-modern">
                <button type="submit" class="btn-modern-danger btn-modern-lg">Delete Sale</button>
                <a asp-action="Index" class="btn-modern-outline btn-modern-lg">Cancel</a>
            </div>
        </form>
    </div>
</div>

@section Styles {
    <link rel="stylesheet" href="~/css/pages/create-product.css" />
}