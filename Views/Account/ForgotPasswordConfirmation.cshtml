@using ClothInventoryApp.Resources
@using Microsoft.Extensions.Localization
@using System.Globalization
@inject IStringLocalizer<SharedResource> Localizer
@{
    Layout = null;
    ViewData["Title"] = Localizer["Request Sent"];
}

<!DOCTYPE html>
<html lang="@CultureInfo.CurrentUICulture.Name">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/png" href="~/images/websitelogo.png" asp-append-version="true" />
    <title>@Localizer["Request Sent"] - StockEasy</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/auth.css" asp-append-version="true" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet" />
</head>
<body class="auth-body-modern @(CultureInfo.CurrentUICulture.Name.StartsWith("my") ? "culture-my" : "")">

    <div class="auth-bg-shape auth-bg-shape-1"></div>
    <div class="auth-bg-shape auth-bg-shape-2"></div>

    <div class="auth-page auth-page-centered">
        <div class="auth-panel auth-panel-narrow">
            <div class="auth-card-modern text-center">
                <div class="auth-card-header">
                    <div class="auth-card-avatar auth-card-avatar-success">
                        <i class="bi bi-check-circle-fill auth-icon-success-lg"></i>
                    </div>
                    <h2>@Localizer["Request Received"]</h2>
                    <p class="auth-copy-spaced">
                        @Localizer["If the email exists in StockEasy, a password reset link will be sent shortly. Please check your inbox and spam folder."]
                    </p>
                </div>

                <div class="auth-form auth-form-compact-top">
                    <a asp-action="Login" class="btn auth-submit-btn">
                        <i class="bi bi-arrow-left"></i>
                        <span>@Localizer["Back to Sign In"]</span>
                    </a>
                </div>

                <div class="auth-card-footer">
                    <i class="bi bi-shield-check"></i>
                    @Localizer["Your data is protected and tenant-isolated."]
                </div>
            </div>
        </div>
    </div>

</body>
</html>
